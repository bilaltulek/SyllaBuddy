# SyllaBuddy
code was built on pycharm, cannot guarantee it will run on other IDEs as we had 
some trouble with VScode. set src as root directory using the "mark directory as" button when you right click the src folder(its at the bottom). 
enter a gemini key in the .env file to get full functionality of the parser. 